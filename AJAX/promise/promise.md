1.单线程模型
js在一个线程上运行，js同时只能执行一个任务，其他任务需要排队等候。
2.任务
同步任务：在主线程上排队执行的任务。只有当前任务执行完才能执行下一个任务。
异步任务：不进入主线程而是进入任务队列的任务。引擎认为异步任务可以执行了就会放入主线程执行。
如ajax从服务器得到了结果，该任务以回调函数的形式进入主线程执行。
3.任务队列
js在运行时，除了一个正在运行的主线程，引擎还提供一个任务队列，存放各种异步任务。
4.异步操作模式
回调函数、事件监听、发布/订阅（观察者模式）